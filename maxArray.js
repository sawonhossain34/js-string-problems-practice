// maxx  array

function maxArray(numbers){
    let tallest = numbers[0];
for(i = 0 ; i < numbers.length; i++){
    const index = i ;
    console.log(index);
    const element = numbers[index];
    // console.log(element);
}
return tallest;
}

const heights = [167,190,156,178,193];
const tall =maxArray(heights);
// console.log(tall);